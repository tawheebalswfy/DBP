name: RDP + MT5 PORTABLE

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 350

    steps:
      - name: Enable RDP
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: Create User
        shell: pwsh
        run: |
          $pass = "P@ssw0rd123!"
          net user RDP $pass /add
          net localgroup administrators RDP /add
          net localgroup "Remote Desktop Users" RDP /add
          echo "RDP_USER=RDP" >> $env:GITHUB_ENV
          echo "RDP_PASS=$pass" >> $env:GITHUB_ENV

      - name: Download MT5 Portable
        shell: pwsh
        run: |
          $zipUrl = "https://download.mql5.com/cdn/web/metaquotes.software.corp/mt5/mt5portable.zip"
          $zipPath = "C:\Users\RDP\Desktop\mt5.zip"

          Invoke-WebRequest -Uri $zipUrl -OutFile $zipPath

          Expand-Archive $zipPath -DestinationPath "C:\Users\RDP\Desktop\MT5" -Force

      - name: Run MT5 + Login
        shell: pwsh
        run: |
          $terminal = "C:\Users\RDP\Desktop\MT5\terminal64.exe"

          if (!(Test-Path $terminal)) {
            Write-Host "MT5 NOT FOUND ❌"
            exit 1
          }

          $config = @"
[Common]
Login=1069352
Password=#wU3Q!Xp
Server=VTMarkets-Demo
"@

          $configPath = "$env:TEMP\config.ini"
          $config | Out-File -FilePath $configPath -Encoding ASCII

          Start-Process -FilePath $terminal -ArgumentList "/portable /config:$configPath"

          Write-Host "MT5 PORTABLE RUNNING ✅"

      - name: Install Tailscale
        shell: pwsh
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $file = "$env:TEMP\ts.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet" -Wait

      - name: Connect Tailscale
        shell: pwsh
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $exe = "C:\Program Files\Tailscale\tailscale.exe"
          & $exe up --authkey=$env:TAILSCALE_AUTH_KEY

          $ip = (& $exe ip -4 | Select-Object -First 1)
          echo "IP=$ip" >> $env:GITHUB_ENV

      - name: Show Info
        shell: pwsh
        run: |
          Write-Host "IP: $env:IP"
          Write-Host "User: $env:RDP_USER"
          Write-Host "Pass: $env:RDP_PASS"

      - name: Keep Alive
        shell: pwsh
        run: |
          Start-Sleep -Seconds 20000
